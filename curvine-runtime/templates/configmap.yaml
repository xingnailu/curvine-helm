{{- include "curvine.validateMasterReplicas" . }}
{{- include "curvine.validateTransfer" . }}
{{- include "curvine.validateWebAdmin" . }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "curvine.fullname" . }}-config
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "curvine.labels" . | nindent 4 }}
    app.kubernetes.io/component: config
data:
  curvine-env.sh: |
    #!/bin/bash
    export CURVINE_HOME="$(cd "$(dirname "$0")"/..; pwd)"

    OS_NAME=$(uname)
    # Get the IP address from hostname, taking the last network interface address
    LOCAL_HOSTNAME="$(hostname || echo localhost)"

    if [ "$OS_NAME" == "Linux" ]; then
        LOCAL_IP=$(hostname -I | awk '{print $NF}')
    elif [ "$OS_NAME" == "Darwin" ]; then
        LOCAL_IP=$(ifconfig | grep "inet " | awk '{print $2}' | grep -v 127.0.0.1 | tail -n 1)
    else
        echo "Unsupported OS: $OS_NAME"
        exit 1
    fi

    # master bound host name
    export CURVINE_MASTER_HOSTNAME=${CURVINE_MASTER_HOSTNAME:-$LOCAL_HOSTNAME}

    # worker bound host name
    export CURVINE_WORKER_HOSTNAME=${CURVINE_WORKER_HOSTNAME:-$LOCAL_IP}

    # The client server hostname is used to determine whether the worker and client are on the same machine.
    export CURVINE_CLIENT_HOSTNAME=${CURVINE_CLIENT_HOSTNAME:-$LOCAL_HOSTNAME}

    export CURVINE_CONF_FILE=${CURVINE_CONF_FILE:-$CURVINE_HOME/conf/curvine-cluster.toml}
  curvine-cluster.toml: |
    cluster_id = "{{ .Values.cluster.id }}"
    format_master = {{ .Values.cluster.formatMaster | default false }}
    format_worker = {{ .Values.cluster.formatWorker | default false }}
    enable_s3_gateway = {{ .Values.worker.s3Gateway.enabled | default false }}
    {{- $logDir := .Values.config.log.logDir }}
    {{- if .Values.config.log.console }}
    {{- $logDir = "stdout" }}
    {{- end }}

    {{- $metaDir := .Values.config.master.metaDir }}
    {{- $metaMount := .Values.master.storage.meta.mountPath }}
    {{- if or (eq $metaDir "") (eq $metaDir nil) }}
    {{- $metaDir = printf "%s/db" (trimSuffix "/" $metaMount) }}
    {{- end }}
    {{- $metaDir = include "curvine.absPath" (dict "path" $metaDir) }}
    {{- $metaMountAbs := include "curvine.absPath" (dict "path" $metaMount) }}
    {{- if eq $metaDir $metaMountAbs }}
    {{- $metaDir = printf "%s/db" (trimSuffix "/" $metaDir) }}
    {{- end }}

    {{- $journalDir := .Values.config.journal.journalDir }}
    {{- $journalMount := .Values.master.storage.journal.mountPath }}
    {{- if or (eq $journalDir "") (eq $journalDir nil) }}
    {{- $journalDir = printf "%s/db" (trimSuffix "/" $journalMount) }}
    {{- end }}
    {{- $journalDir = include "curvine.absPath" (dict "path" $journalDir) }}
    {{- $journalMountAbs := include "curvine.absPath" (dict "path" $journalMount) }}
    {{- if eq $journalDir $journalMountAbs }}
    {{- $journalDir = printf "%s/db" (trimSuffix "/" $journalDir) }}
    {{- end }}

    [master]
    rpc_host = "0.0.0.0"
    meta_dir = "{{ $metaDir }}"
    rpc_port = {{ .Values.master.rpcPort }}
    web_port = {{ .Values.master.webPort }}
    web1_port = {{ .Values.master.web1Port }}
    {{- if .Values.configOverrides.master }}
    {{- range $key, $value := .Values.configOverrides.master }}
    {{- if not (and $.Values.config.log.console (eq $key "log.log_dir")) }}
    {{ $key }} = {{ toJson $value }}
    {{- end }}
    {{- end }}
    {{- end }}
    {{- if .Values.config.log.console }}
    log.log_dir = "stdout"
    {{- end }}

    [journal]
    enable = {{ .Values.config.journal.enable }}
    rpc_port = {{ .Values.master.journalPort }}
    journal_dir = "{{ $journalDir }}"
    snapshot_interval = {{ .Values.config.journal.snapshotInterval | quote }}
    snapshot_entries = {{ include "curvine.tomlInt" .Values.config.journal.snapshotEntries }}
    {{- if .Values.configOverrides.journal }}
    {{- range $key, $value := .Values.configOverrides.journal }}
    {{ $key }} = {{ toJson $value }}
    {{- end }}
    {{- end }}

    {{- range $i := until (int .Values.master.replicas) }}
    [[journal.journal_addrs]]
    id = {{ add $i 1 }}
    hostname = "{{ include "curvine.masterFullname" $ }}-{{ $i }}.{{ include "curvine.masterServiceName" $ }}.{{ $.Release.Namespace }}.svc.{{ $.Values.global.clusterDomain }}"
    port = {{ $.Values.master.journalPort }}
    {{- end }}

    [worker]
    rpc_host = "0.0.0.0"
    rpc_port = {{ .Values.worker.rpcPort }}
    web_port = {{ .Values.worker.webPort }}
    data_dir = {{ include "curvine.workerDataDirs" . | trim }}
    enable_s3_gateway = {{ .Values.worker.s3Gateway.enabled | default false }}
    enable_distributed_auth = {{ .Values.worker.s3Gateway.enableDistributedAuth | default true }}
    {{- if .Values.configOverrides.worker }}
    {{- range $key, $value := .Values.configOverrides.worker }}
    {{- if not (and $.Values.config.log.console (eq $key "log.log_dir")) }}
    {{ $key }} = {{ toJson $value }}
    {{- end }}
    {{- end }}
    {{- end }}
    {{- if .Values.config.log.console }}
    log.log_dir = "stdout"
    {{- end }}

    [s3_gateway]
    enable = {{ .Values.worker.s3Gateway.enabled | default false }}
    listen = "{{ .Values.worker.s3Gateway.listen }}"
    {{- if .Values.worker.s3Gateway.enabled }}
    # Use Curvine filesystem for multipart upload part files to enable distributed access
    put_temp_dir = "/system/s3-temp"
    {{- else }}
    put_temp_dir = "/tmp/curvine-temp"
    {{- end }}

    [client]
    block_size_str = "{{ .Values.config.client.blockSizeStr }}"
    {{- if .Values.configOverrides.client }}
    {{- range $key, $value := .Values.configOverrides.client }}
    {{ $key }} = {{ toJson $value }}
    {{- end }}
    {{- end }}

    {{- range $i := until (int .Values.master.replicas) }}
    [[client.master_addrs]]
    hostname = "{{ include "curvine.masterFullname" $ }}-{{ $i }}.{{ include "curvine.masterServiceName" $ }}.{{ $.Release.Namespace }}.svc.{{ $.Values.global.clusterDomain }}"
    port = {{ $.Values.master.rpcPort }}
    {{- end }}

    [transfer]
    enabled = {{ .Values.transfer.enabled }}
    hostname = "{{ include "curvine.transferServiceName" . }}.{{ .Release.Namespace }}.svc.{{ .Values.global.clusterDomain }}"
    rpc_port = {{ .Values.transfer.rpcPort }}
    web_port = {{ .Values.transfer.webPort }}
    {{- with .Values.transfer.storeUrl }}
    store_url = {{ . | quote }}
    {{- end }}
    {{- $managedTransferKeys := list "enabled" "hostname" "rpc_port" "web_port" "store_url" }}
    {{- if .Values.configOverrides.transfer }}
    {{- range $key, $value := .Values.configOverrides.transfer }}
    {{- if has $key $managedTransferKeys }}
    {{- fail (printf "configOverrides.transfer.%s is managed by the transfer section" $key) }}
    {{- end }}
    {{ $key }} = {{ toJson $value }}
    {{- end }}
    {{- end }}

    [web]
    hostname = "0.0.0.0"
    port = {{ .Values.webAdmin.port }}
    {{- $managedWebKeys := list "hostname" "port" }}
    {{- if .Values.configOverrides.web }}
    {{- range $key, $value := .Values.configOverrides.web }}
    {{- if has $key $managedWebKeys }}
    {{- fail (printf "configOverrides.web.%s is managed by the webAdmin section" $key) }}
    {{- end }}
    {{ $key }} = {{ toJson $value }}
    {{- end }}
    {{- end }}

    [log]
    level = "{{ .Values.config.log.level }}"
    log_dir = "{{ $logDir }}"
    {{- if .Values.configOverrides.log }}
    {{- range $key, $value := .Values.configOverrides.log }}
    {{- if not (and $.Values.config.log.console (eq $key "log_dir")) }}
    {{ $key }} = {{ toJson $value }}
    {{- end }}
    {{- end }}
    {{- end }}
